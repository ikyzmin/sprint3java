# 🎮 Задание: "Гильдия Героев" - Практика ООП в Java

## 📖 Описание мира

Вы — разработчик игровой системы для фэнтезийной RPG. В мире существуют разные типы героев, обладающих уникальными способностями. Ваша задача — создать иерархию классов, используя принципы объектно-ориентированного программирования: наследование, переопределение методов, модификаторы доступа и интерфейсы.

## 🎯 Цели обучения

- Практика создания иерархии классов с помощью наследования
- Понимание и применение модификаторов доступа (private, protected, public)
- Переопределение методов в классах-наследниках
- Реализация интерфейсов для обеспечения полиморфизма
- Понимание различий между абстрактными классами и интерфейсами

## 📋 Часть 1: Базовый класс Hero

Создайте базовый класс `Hero`, который будет представлять общего предка для всех типов героев.

### Характеристики класса Hero:

**Поля:**
- `name` (String) — имя героя, доступно только внутри класса Hero
- `health` (int) — текущее здоровье, доступно в классе Hero и его наследниках
- `baseDamage` (int) — базовый урон, доступно только внутри класса Hero

**Конструктор:**
- Принимает три параметра: имя, здоровье и базовый урон
- Инициализирует все поля класса

**Методы:**
- `introduce()` — выводит приветственное сообщение (публичный метод)
- `attack()` — возвращает значение урона и описывает атаку (публичный метод)
- `takeDamage(int damage)` — уменьшает здоровье на указанное значение (защищенный метод)
- `isAlive()` — проверяет, жив ли герой (публичный метод)
- Геттер для поля `name` (публичный метод)

## 📋 Часть 2: Специализированные классы героев

Создайте два класса-наследника от `Hero`, которые будут представлять специализированные типы героев.

### Класс Warrior (Воин)

Воин — специалист по ближнему бою с высокой защитой.

**Дополнительные поля:**
- `armor` (int) — значение брони, доступно только внутри класса Warrior

**Особенности:**
- Конструктор принимает дополнительный параметр — значение брони
- Переопределяет метод `attack()` для нанесения усиленного урона
- Переопределяет метод `takeDamage()` для учета брони при получении урона

### Класс Mage (Маг)

Маг — мастер магии, наносящий высокий урон, но уязвимый в ближнем бою.

**Дополнительные поля:**
- `mana` (int) — текущий запас маны, доступно только внутри класса Mage
- `maxMana` (int) — максимальный запас маны, доступно только внутри класса Mage

**Особенности:**
- Конструктор принимает дополнительный параметр — максимальный запас маны
- Переопределяет метод `attack()` для использования магических атак (при наличии маны)
- Переопределяет метод `introduce()` для добавления информации о магических способностях

## 📋 Часть 3: Интерфейсы для расширения возможностей

Создайте интерфейсы, которые будут определять дополнительные способности героев.

### Интерфейс MagicUser

Определяет контракт для существ, использующих магию.

**Методы интерфейса:**
- `getCurrentMana()` — возвращает текущий запас маны
- `canCastSpell(int manaCost)` — проверяет, достаточно ли маны для заклинания
- `restoreMana(int amount)` — восстанавливает ману

### Интерфейс Tank

Определяет контракт для существ с высокой защитой.

**Методы интерфейса:**
- `getArmorValue()` — возвращает текущее значение брони
- `fortify()` — временно увеличивает защиту
- `canBlock()` — проверяет, может ли блокировать атаки

## 📋 Часть 4: Гибридный класс Paladin

Создайте класс `Paladin`, который демонстрирует множественное наследование через интерфейсы.

**Особенности класса Paladin:**
- Наследуется от класса `Hero`
- Реализует оба интерфейса: `MagicUser` и `Tank`
- Имеет уникальные способности, сочетающие магию и защиту
- Переопределяет метод `attack()` для использования благословенного оружия

## 📋 Часть 5: Система боя

Создайте класс `GuildBattle` для тестирования созданной системы.

**Требования к GuildBattle:**
- Создание нескольких героев разных типов
- Демонстрация полиморфизма через массивы интерфейсов
- Моделирование простого боя между героями
- Проверка всех реализованных способностей

## 🧠 Контрольные вопросы

### По модификаторам доступа:
1. Почему поле `name` в классе `Hero` должно быть приватным?
2. Какая разница между модификаторами `protected` и `private`?
3. Почему метод `takeDamage()` имеет модификатор `protected`, а не `public`?

### По наследованию:
1. Какие методы и поля наследуют классы `Warrior` и `Mage` от `Hero`?
2. Может ли класс `Warrior` напрямую обращаться к полю `baseDamage` родительского класса?

### По переопределению методов:
1. Что произойдет, если в классе `Mage` не переопределить метод `introduce()`?
2. Как вызвать родительскую версию переопределенного метода?

### По интерфейсам:
1. Чем интерфейс отличается от абстрактного класса?
2. Почему класс `Paladin` может реализовать два интерфейса, но не может унаследоваться от двух классов?
3. Что такое "программирование на уровне интерфейсов" и какие преимущества оно дает?

### По полиморфизму:
1. Что выведет код, если создать объект `Mage` и присвоить его переменной типа `Hero`?
2. Как интерфейсы помогают реализовать полиморфизм?

## ✅ Критерии успешного выполнения

1. **Корректность ООП-структуры:**
   - Правильное использование наследования
   - Адекватное применение модификаторов доступа
   - Корректное переопределение методов

2. **Работоспособность системы:**
   - Все классы компилируются без ошибок
   - Герои могут атаковать и получать урон
   - Особые способности работают согласно описанию

3. **Полиморфизм:**
   - Возможность хранить разные типы героев в общих структурах
   - Корректная работа методов через интерфейсы

4. **Чистота кода:**
   - Логичные имена классов, методов и переменных
   - Отсутствие дублирования кода
   - Правильное использование ключевого слова `super`

## 💡 Советы для начинающих

1. Начните с создания базового класса `Hero` и убедитесь, что он работает корректно
2. Постепенно добавляйте классы-наследники, тестируя каждый шаг
3. Сначала реализуйте простую версию интерфейсов, затем усложняйте
4. Используйте аннотацию `@Override` для переопределяемых методов
5. Помните, что интерфейсы могут содержать только константы и абстрактные методы (в версиях Java до 8)

Удачи в создании вашей гильдии героев! 🛡️⚔️🔮